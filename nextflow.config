/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    AMR Trinity Nextflow config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/
manifest {
    name            = 'amrtrinity' 
    author          = "Seqafrica"
    homePage        = 'https://github.com/Osadio95/amrtrinity'
    description     = "Antimicrobial resistance detection using AMRFinderPlus, ResFinder and RGI with harmonization"
    mainScript      = 'main.nf'
    nextflowVersion = '>=20.10.0'
    version         = '1.0.11'
}

// Global default params, used in configs
params {
    // Options: Mandatory
    input                      = './test/mini/isolates.tsv'
    
    // Options: AMR Analysis
    threads                    = 4
    
    
    outdir                     = './results'
    


// Process configuration
process {
    shell = ['/bin/bash', '-euo', 'pipefail']
    
    withName: 'amrfinderplus' {
        container = 'docker.io/ncbi/amr:4.0.23-2025-07-16.1'
        cpus = { check_max( params.threads ?: 4, 8 ) }
    }
    withName: 'resfinder' {
        container = 'docker.io/genomicepidemiology/resfinder:4.7.2'
        containerOptions = '--entrypoint ""'
        cpus = { check_max( params.threads ?: 4, 8 ) }
    }
    withName: 'rgi' {
        container = 'quay.io/biocontainers/rgi:6.0.5--pyh05cac1d_0'
        cpus = { check_max( params.threads ?: 4, 8 ) }
    }
    withName: 'hamronize' {
        container = 'ghcr.io/zwets/hamronization:1.1.10'
        containerOptions = '--entrypoint ""'
        cpus = 1
    }
    withName: 'summarise' {
        container = 'ghcr.io/zwets/hamronization:1.1.10'
        containerOptions = '--entrypoint ""'
        cpus = 1
    }
}

// Profiles - MUST HAVE this structure
profiles {
    docker {
        docker.enabled = true
        docker.userEmulation = true
    }
    singularity {
        singularity.enabled = true
        singularity.autoMounts = true
    }
    standard {
        docker.enabled = true
    }
}

// REQUIRED: Environment variables
env {
    PYTHONNOUSERSITE = 1
    R_PROFILE_USER   = "/.Rprofile"
    R_ENVIRON_USER   = "/.Renviron"
}

// REQUIRED: Shell configuration
process.shell = ['/bin/bash', '-euo', 'pipefail']

// REQUIRED: Reporting sections with timestamp
def trace_timestamp = new java.util.Date().format( 'yyyy-MM-dd_HH-mm-ss')
timeline {
    enabled = true
    file    = "${params.tracedir}/execution_timeline_${trace_timestamp}.html"
}
report {
    enabled = true
    file    = "${params.tracedir}/execution_report_${trace_timestamp}.html"
}
trace {
    enabled = true
    file    = "${params.tracedir}/execution_trace_${trace_timestamp}.txt"
}
dag {
    enabled = true
    file    = "${params.tracedir}/pipeline_dag_${trace_timestamp}.html"
}

// REQUIRED: check_max function for nf-core compatibility
def check_max(obj, type) {
    if (type == 'memory') {
        try {
            if (obj.compareTo('128.GB' as nextflow.util.MemoryUnit) == 1)
                return '128.GB' as nextflow.util.MemoryUnit
            else
                return obj
        } catch (all) {
            println "   ### ERROR ###   Max memory '128.GB' is not valid! Using default value: $obj"
            return obj
        }
    } else if (type == 'time') {
        try {
            if (obj.compareTo('240.h' as nextflow.util.Duration) == 1)
                return '240.h' as nextflow.util.Duration
            else
                return obj
        } catch (all) {
            println "   ### ERROR ###   Max time '240.h' is not valid! Using default value: $obj"
            return obj
        }
    } else if (type == 'cpus') {
        try {
            return Math.min( obj, 16 )
        } catch (all) {
            println "   ### ERROR ###   Max cpus '16' is not valid! Using default value: $obj"
            return obj
        }
    }
}
